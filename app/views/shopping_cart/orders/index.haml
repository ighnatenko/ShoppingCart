%h1.mt-0.mb-25= t('orders.title')
%p.lead.small.mb-10.visible-xs= t('orders.sort_by')
.dropdowns.dropdown.general-order-dropdown
  %a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
    = params[:state] ? t("orders.#{params[:state].to_s}") : t('orders.all')  
    %i.fa.fa-angle-down.dropdown-icon.pull-right
  %ul.dropdown-menu
    %li= link_to t('orders.all'), shopping_cart.orders_path
    - ShoppingCart::Order.aasm_states.each do |state|
      %li= link_to t("orders.#{state.to_s}"), shopping_cart.orders_path(state: state)    
= render 'order_items', orders: @orders